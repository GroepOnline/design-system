# Signaal composition patterns

Patterns describe proven combinations of catalog components. They are optional recipes, not components and not runtime dependencies.

A pattern may recommend one or more design profiles. Products can adopt, adapt or ignore a pattern without changing Signaal core.
