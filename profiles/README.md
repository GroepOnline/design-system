# Signaal design profiles

Profiles are optional design/taste overlays for a class of surface. They are not products, skins or runtime dependencies.

Resolution order is `base -> profile -> extension`. A consumer may select neither, one, or both. The core catalog never imports a profile or extension.
